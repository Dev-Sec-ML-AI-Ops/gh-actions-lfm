name: HR Leave Management Clean CI/CD

on:
  push:
    branches: [main]
    paths:
      - "HR.LeaveManagement.Clean/**"
      - ".github/workflows/HR-LeaveManagement-Clean.yaml"
  pull_request:
    branches: [main]
    paths:
      - "HR.LeaveManagement.Clean/**"
      - ".github/workflows/HR-LeaveManagement-Clean.yaml"
  workflow_dispatch:

concurrency:
  group: hr-leavemanagement-clean-${{ github.ref }}
  cancel-in-progress: true

env:
  DOTNET_VERSION: "8.0.x"
  SOLUTION_PATH: HR.LeaveManagement.Clean/HR.LeaveManagement.Clean.sln
  API_PROJECT_PATH: HR.LeaveManagement.Clean/HR.LeaveManagement.Api/HR.LeaveManagement.Api.csproj
  BUILD_CONFIGURATION: Release
  PUBLISH_DIR: ./publish
  # Optional CLI log level for dotnet commands:
  # quiet | minimal | normal | detailed | diagnostic
  # DOTNET_VERBOSITY: diagnostic

jobs:
  build-test-publish:
    name: Build, Test, and Publish
    runs-on: ubuntu-latest
    # Optional debug flags to set in GitHub Actions variables/secrets:
    # ACTIONS_STEP_DEBUG=true
    # ACTIONS_RUNNER_DEBUG=true

    steps:
      - name: List .NET SDK
        run: dotnet --list-sdks 

      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Show .NET info
        run: dotnet --info

      # Optional: uncomment to print whether GitHub debug flags are enabled.
      # - name: Show debug flags
      #   run: |
      #     echo "ACTIONS_STEP_DEBUG=${ACTIONS_STEP_DEBUG}"
      #     echo "ACTIONS_RUNNER_DEBUG=${ACTIONS_RUNNER_DEBUG}"

      - name: Cache NuGet packages
        uses: actions/cache@v5
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('HR.LeaveManagement.Clean/**/*.csproj', 'HR.LeaveManagement.Clean/**/*.props', 'HR.LeaveManagement.Clean/**/*.targets', 'HR.LeaveManagement.Clean/HR.LeaveManagement.Clean.sln') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      - name: Restore dependencies
        run: dotnet restore ${{ env.SOLUTION_PATH }}
        # Example verbose restore:
        # run: dotnet restore ${{ env.SOLUTION_PATH }} --verbosity ${{ env.DOTNET_VERBOSITY }}

      - name: Clean solution
        run: dotnet clean ${{ env.SOLUTION_PATH }} --configuration ${{ env.BUILD_CONFIGURATION }}

      - name: Build solution
        run: dotnet build ${{ env.SOLUTION_PATH }} --configuration ${{ env.BUILD_CONFIGURATION }} --no-restore
        # Example verbose build:
        # run: dotnet build ${{ env.SOLUTION_PATH }} --configuration ${{ env.BUILD_CONFIGURATION }} --no-restore --verbosity ${{ env.DOTNET_VERBOSITY }}

      - name: Run unit and integration tests
        run: >
          dotnet test ${{ env.SOLUTION_PATH }}
          --configuration ${{ env.BUILD_CONFIGURATION }}
          --no-build
          --logger "trx;LogFileName=test-results.trx"
          --collect:"XPlat Code Coverage"
          --results-directory ./TestResults
        # Example verbose test:
        # run: >
        #   dotnet test ${{ env.SOLUTION_PATH }}
        #   --configuration ${{ env.BUILD_CONFIGURATION }}
        #   --no-build
        #   --verbosity ${{ env.DOTNET_VERBOSITY }}

      - name: Publish API project
        run: >
          dotnet publish ${{ env.API_PROJECT_PATH }}
          --configuration ${{ env.BUILD_CONFIGURATION }}
          --no-build
          --output ${{ env.PUBLISH_DIR }}

      - name: Upload published artifact
        uses: actions/upload-artifact@v6
        with:
          name: hr-leavemanagement-clean-api
          path: ${{ env.PUBLISH_DIR }}
          if-no-files-found: error
          retention-days: 7

      - name: Upload test results artifact
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: hr-leavemanagement-clean-test-results
          path: ./TestResults
          if-no-files-found: warn
          retention-days: 7

  # deploy-dev:
  #   name: Deploy to Development
  #   needs: build-test-publish
  #   if: github.ref == 'refs/heads/main'
  #   runs-on: ubuntu-latest
  #   environment:
  #     name: development
  #     url: https://your-dev-url.example.com
  #   steps:
  #     - name: Download published artifact
  #       uses: actions/download-artifact@v4
  #       with:
  #         name: hr-leavemanagement-clean-api
  #         path: ./deploy-package
  #
  #     - name: Deploy application
  #       run: echo "Add your deployment script here"
  #       # Examples:
  #       # Azure Web App:
  #       # uses: azure/webapps-deploy@v3
  #       # with:
  #       #   app-name: your-app-name
  #       #   publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
  #       #   package: ./deploy-package
  #
  # deploy-prod:
  #   name: Deploy to Production
  #   needs: deploy-dev
  #   if: github.ref == 'refs/heads/main'
  #   runs-on: ubuntu-latest
  #   environment:
  #     name: production
  #     url: https://your-prod-url.example.com
  #   steps:
  #     - name: Download published artifact
  #       uses: actions/download-artifact@v4
  #       with:
  #         name: hr-leavemanagement-clean-api
  #         path: ./deploy-package
  #
  #     - name: Deploy application
  #       run: echo "Add your production deployment script here"
