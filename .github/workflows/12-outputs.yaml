name: 12 - Working with Outputs

on: 
  pull_request:
  workflow_dispatch:
    inputs:
        build-status: 
            type: choice
            options:
             - success
             - failure
            description: Stting build status
            default: success

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
        build-status: ${{steps.build.outputs.status}}
    steps:
        - name: Print GITHUB_OUTPUT path
          run: |
            echo GITHUB OUTPUT Path: "$GITHUB_OUTPUT"
        
        - name: Build
          id: build
          run: |
            echo status=${{ inputs.build-status }} >> "$GITHUB_OUTPUT"
  deploy:
    runs-on: ubuntu-latest
    needs: build
    if: ${{ contains(needs.build.outputs.build-status,'success')}}
    steps:
        - name: Deploy
          run: echo Deploying